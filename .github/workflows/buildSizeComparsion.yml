name: Secrets example
on: push
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: Test
        run: |
          echo "$CANDY_SECURE"

        env: 
          CANDY_SECURE: ${{ secrets.TOKEN }}